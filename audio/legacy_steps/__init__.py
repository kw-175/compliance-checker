"""Legacy audio workflow steps kept for compatibility.

The active audio pipeline now focuses on privacy and content-safety detection.
Older source-scanning, keyword/regex, deduplication, redaction-rendering, and
release-package steps are exposed here so future expansion can reuse them
without reshaping the active pipeline.
"""
