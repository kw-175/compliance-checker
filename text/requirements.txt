# ──────────────────────────────────────────────────────────────
# Text Data Compliance Checker – Python Dependencies
# ──────────────────────────────────────────────────────────────

# ── Core ──────────────────────────────────────────────────────
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
httpx>=0.27,<1.0
pyyaml>=6.0,<7.0
python-multipart>=0.0.9,<1.0

# ── Step C: Text Extraction ─────────────────────────────────
trafilatura>=1.8,<2.0
PyMuPDF>=1.23,<2.0
langdetect>=1.0,<2.0

# ── Step D: Deduplication (fallback from Duplodocus) ────────
datasketch>=1.6,<2.0

# ── Step E1a: Keyword Scan ──────────────────────────────────
flashtext2>=1.0,<2.0

# ── Step E1b: Regex Scan ────────────────────────────────────
# hyperscan>=0.7,<1.0       # Uncomment on Linux with Vectorscan installed

# ── Step F: Privacy Detection ───────────────────────────────
presidio-analyzer>=2.2,<3.0
presidio-anonymizer>=2.2,<3.0
spacy>=3.7,<4.0
# After installing: python -m spacy download en_core_web_sm
transformers>=4.40,<5.0
torch>=2.0

# ── Step G: Safety Moderation ───────────────────────────────
# (uses transformers + torch from above)

# ── Step J: Lineage ────────────────────────────────────────
openlineage-python>=1.10,<2.0

# ── Dev / Test ─────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
