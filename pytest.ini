[pytest]
markers =
    integration: tests that exercise full picture pipeline, API background work, or service-style orchestration.
    slow: tests that may take noticeable time and should not run in the default quick loop.
addopts = -m "not integration and not slow"
